import Form from "./Form/Form";
import styles from "./SectionForm.module.css";

const SectionForm = () => {
  return (
    <section>
      <h2>Contato</h2>
      <Form />
    </section>
  );
};

export default SectionForm;
